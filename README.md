1: SQL конфиг моей БД является init.sql в /api

2: Переименуйте .env.example в .env. 
Потом заполните .env вашими данными для подключения к базе данных 
(Без кавычек, просто текст).
(Без пробела у знака равно, пример - DB_HOST=localhost)

3: Запустите Npm run dev